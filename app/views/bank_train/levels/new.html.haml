.page-levels-new
  %h3 创建级别
  %a{:role=>"button",class: "btn btn-primary",:href=>"/levels "}级别清单

  = simple_form_for @level do |f|
    = f.input :number

    = f.input :name

    = f.association :posts, label_method: :name, as: :check_boxes

    = f.button :submit, "添加"